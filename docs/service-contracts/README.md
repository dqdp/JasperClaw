# Service Contracts

Purpose:
This directory defines the canonical interfaces that runtime code is expected to implement or consume.

Start here:
- `agent-api.md`: open first for the main public backend contract.

Index:
- `agent-api.md`: open when implementing or consuming the OpenAI-compatible control-plane surface.
- `tools.md`: open when implementing the internal typed tools boundary and its policy/audit rules.
