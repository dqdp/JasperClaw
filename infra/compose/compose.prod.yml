services:
  open-webui:
    restart: unless-stopped

  agent-api:
    restart: unless-stopped

  ollama:
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  postgres:
    restart: unless-stopped
