# Caddy

Purpose:
This directory holds the reverse-proxy configuration for the public ingress path.

Start here:
- `Caddyfile`: open when changing host routing, TLS termination, or upstream service exposure.

Index:
- `Caddyfile`: open when adding or changing externally exposed routes such as WebUI, `agent-api`, or Telegram webhook paths.
