# Telegram Ingress Core

Purpose:
This directory contains service-wide config and structured logging helpers.

Start here:
- `config.py`: open first when a change introduces or consumes Telegram ingress settings.

Index:
- `config.py`: open when changing env parsing, defaults, or operational enablement rules.
- `logging.py`: open when changing request IDs or structured ingress log events.
