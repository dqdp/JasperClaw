[project]
name = "telegram-ingress"
version = "0.1.0"
description = "Telegram ingress bridge for local-assistant"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
  "httpx>=0.28.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
