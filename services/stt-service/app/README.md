# STT Service App

Purpose:
This directory contains the current placeholder implementation for speech-to-text service behavior.

Start here:
- `main.py`: open when changing the current stub endpoint behavior.

Index:
- `main.py`: open when changing placeholder liveness or future STT route wiring.
