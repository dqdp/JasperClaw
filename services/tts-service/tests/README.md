# TTS Service Tests

Purpose:
This directory contains the minimal placeholder tests for `tts-service`.

Start here:
- `test_health.py`: open when changing the current stub health contract.

Index:
- `test_health.py`: open when changing liveness expectations for the placeholder service.
