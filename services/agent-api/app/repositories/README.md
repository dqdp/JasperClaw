# Agent API Repositories

Purpose:
This directory contains persistence interfaces and concrete Postgres-backed implementations.

Start here:
- `postgres.py`: open first for canonical chat, retrieval, and tool-audit storage behavior.

Index:
- `postgres.py`: open when changing conversation resolution, persistence writes, retrieval queries, or audit storage.
