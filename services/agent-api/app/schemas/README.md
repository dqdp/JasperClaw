# Agent API Schemas

Purpose:
This directory contains typed request and response models for the public HTTP surface.

Start here:
- `chat.py`: open first for chat completion request and response validation.

Index:
- `chat.py`: open when changing chat payload shape or streaming chunk models.
- `models.py`: open when changing `/v1/models` response shape.
