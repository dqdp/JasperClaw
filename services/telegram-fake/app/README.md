# Telegram Fake App

Purpose:
This directory contains the fake Telegram Bot API implementation used by smoke tests.

Start here:
- `main.py`: open first for fake `sendMessage`, `setWebhook`, `getUpdates`, and test-control endpoints.

Index:
- `main.py`: open when changing fake Telegram API behavior or inspection state.
