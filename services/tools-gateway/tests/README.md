# Tools Gateway Tests

Purpose:
This directory contains minimal placeholder tests for the legacy `tools-gateway` scaffold.

Start here:
- `test_health.py`: open only when auditing the legacy placeholder contract.

Index:
- `test_health.py`: open only when checking the leftover health test for this non-canonical service.
