# Tools Gateway App

Purpose:
This directory contains the leftover placeholder app for the non-canonical `tools-gateway` scaffold.

Start here:
- `main.py`: open only when auditing legacy scaffold behavior.

Index:
- `main.py`: open only when checking what the placeholder service exposes today.
