# app

Purpose:
Minimal FastAPI surface that mimics the Ollama endpoints required by CI smoke.

Start here:
- `main.py`: open first for `/api/tags`, `/api/chat`, `/api/embed`, and test-state helpers.
